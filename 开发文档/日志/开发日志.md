# 🦊 开发日志 — 狐の工作台 Plugin v0.3 · Fox Finance

> 项目根目录：`.obsidian/plugins/fox-dashboard/`

---

## v0.3.0 — 2026-07-23

> Claudian 开发 · Fox Finance 第一阶段启动

### 阶段目标
将旧的记账原型（settings 内嵌扁平数组 + 内联表单）升级为完整的 Fox Finance 财富系统，基于文件存储的流水账本。

### 模块划分（逐步推进）

| 模块 | 文件 | 状态 | 说明 |
|------|------|------|------|
| ① 数据层 | `finance.ts` | ✅ 完成 | 类型定义、Ledger I/O、Inbox、余额推算、旧数据迁移 |
| ② 快速记账弹窗 | `finance-modal.ts` | ✅ 完成 | 类型/金额/账户/分类/备注 + 保存到 Ledger |
| ③ 财富森林看板 | `finance-view.ts` | ✅ 完成 | 独立全屏视图，账户栏/月度概览/流水列表/Inbox 整理 |
| ④ 工作台卡片 | `view.ts` | ✅ 完成 | 重写为森林状态卡片（概览+账户+最近3笔+双按钮） |
| ⑤ 设置页 + 视图注册 | `main.ts` | ❌ 待做 | 账户管理、分类管理、注册新视图、openFinanceView |
| ⑥ CSS 样式 | `styles.css` | ❌ 待做 | 账户栏、类型徽标、全屏看板、Modal 样式 |
| ⑦ 编译测试 | — | ❌ 待做 | 编译、重载、验证 |

### 文档更新
- [x] 完善开发文档结构，新增 Fox Finance 专项规划
- [x] `CLAUDE.md` 添加文档路径指引
- [ ] 更新 `设计规范.md` 加入财富系统配色
- [ ] 更新 `技术规范.md` 加入文件存储规范

### 待办
- [ ] main.ts：settings 接口升级、注册 VIEW_TYPE_FOX_FINANCE、openFinanceView()
- [ ] styles.css：新增样式
- [ ] 编译测试：build → Ctrl+R → 验证记账流程

---

## v0.3.1 — 2026-07-25

> Claudian 维护 · 输入框样式实验（已回退）

### 内容
- 尝试统一所有输入框样式（快速记录的极透背景 + 全边框 + 绿色聚焦），当日回退
- 尝试对齐 Forest Glass 视觉方案（卡片颜色、输入框金色聚焦、按钮风格），当日回退
- 早起标签 `7:30前` → `8:00前`，已回退

### 结论
- 所有视觉改动已回退到原始状态，净改动为零
- 后续可复用 Forest Glass snippet（`财务系统.txt` 中的 CSS）作为全局主题，不与插件样式冲突
- **记录日期补写**：2026-07-23 Fox Finance Devlog 已补充到 `99-Project/FoxFinance/devlog/`

---

## v0.3.2 — 2026-07-26

> Claudian 维护 · 文档迁移 + Fox Finance v0.4.0 对接准备

### 内容
- **文档迁移**：开发文档复制到 `99-Project/FoxDashboard/`（devlog + docs），插件目录保留原副本
- **Claudian 工作记忆独立**：新建 `99-Project/FoxDashboard/claudian-memory.md`，不再写 `memory.md`
- **CLAUDE.md 修正**：memory.md 归属说明改为「小狐自行维护」

### 待办
- [ ] 工作台财富森林卡片适配 Fox Finance v0.4.0（见新窗口 prompt）
